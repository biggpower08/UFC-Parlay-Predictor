"""Feature-builder placeholder for future dedicated prop models."""

from __future__ import annotations


def build_prop_features(*_args, **_kwargs) -> dict:
    return {
        "status": "not_trained",
        "message": "Dedicated prop-model features are not active yet.",
    }
