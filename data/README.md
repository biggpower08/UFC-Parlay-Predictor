# Data

Production data will move here as the hosted database is introduced.

Current local engine data still lives in:

```text
ufc_predictor/data
```

That keeps the existing model and tests stable while the app shell is being
productionized.
